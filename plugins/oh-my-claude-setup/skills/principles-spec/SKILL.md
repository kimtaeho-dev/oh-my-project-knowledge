---
name: principles-spec
description: |
  .claude/rules/principles/ 하위에 생성할 원칙 파일들의 내용 정의.
  oh-my-claude-setup:init 명령 실행 시 반드시 이 정의를 따라 각 파일을 생성한다.
---

# 원칙 파일 정의 (Principles Spec)

아래 정의된 각 파일을 `.claude/rules/principles/` 디렉토리에 생성한다.
기존 파일이 존재하면 내용이 중복되는 항목은 통합하여 중복 없이 정리한다.

---

## workflow-start.md

```markdown
# 작업 시작 원칙

새 작업 요청을 받으면 아래 순서를 예외 없이 따른다.

1. 현재 브랜치와 상태를 확인한다 (git status, git branch)
2. 요청된 작업 내용을 분석하여 작업 범위와 유형을 스스로 판단한다
3. 사소하지 않은 작업(3단계 이상이거나 새로운 파일/모듈 생성이 필요한 경우)은 계획 모드로 시작한다
   - .claude/tasks/plans/{브랜치명}.md 파일을 새로 생성하고 체크 가능한 항목으로 계획을 작성한다
   - 구현 시작 전에 계획을 검토한다
   - 진행하면서 완료 항목을 표시한다
   - 각 단계마다 상위 수준 요약을 제공한다
   - 완료 후 해당 파일에 검토 섹션을 추가한다
4. 판단된 작업 유형에 따라 브랜치명을 결정하고 분기 기준 브랜치를 최신화한 후 작업 브랜치를 생성한다
   - release/hotfix를 제외한 모든 작업: develop을 최신화(git checkout develop → git pull origin develop) 후 브랜치 생성
   - release/hotfix: main을 최신화(git checkout main → git pull origin main) 후 브랜치 생성
```

---

## code-modification.md

```markdown
# 코드 수정 원칙

## 수정 범위 제한

- 요청된 작업 범위 내의 코드만 수정한다
- 요청과 무관한 코드는 어떠한 이유로도 수정하지 않는다
- 개선이 필요해 보이는 코드를 발견하면 언급만 하고 임의로 수정하지 않는다
- 수정이 필요하다고 판단되면 사용자에게 별도 작업으로 제안한다

## 기존 패턴 및 컨벤션 준수

- 기존 패턴, 네이밍 컨벤션, 코드 스타일을 먼저 파악하고 따른다
- 새로운 패턴이나 라이브러리 도입 시 임의로 적용하지 않고 사용자에게 먼저 제안하고 확인을 받는다

## 변경 최소화

- 목적을 달성하는 가장 작은 단위의 변경을 우선으로 한다
- 동일한 결과라면 코드 변경량이 적은 방향을 선택한다
- 기능 구현과 리팩토링을 동시에 진행하지 않는다
- 기능 구현 중 리팩토링이 필요한 코드를 발견하면 현재 작업 완료 후 별도 작업으로 제안한다

## 버그 수정

- 버그 리포트를 받으면 영향 범위와 무관하게 자율적으로 바로 진행한다
- 하나하나 물어보지 않는다
- 로그, 에러, 실패한 테스트를 직접 찾아서 해결한다
- 실패한 CI 테스트는 별도 지시 없이도 직접 찾아서 수정한다
- 임시방편으로 해결하지 않는다. 근본 원인을 찾아서 수정한다

## 파일 탐색 순서

기능 변경 또는 이슈 탐색 시 아래 순서를 따른다.

1. 해당 앱/서비스의 `CLAUDE.md` 내 **도메인 → 파일 위치** 테이블을 먼저 확인한다
2. 테이블에 위치가 명시된 경우 해당 경로로 바로 이동한다
3. 테이블에 없는 경우에만 Glob/Grep으로 탐색한다

## 작업 전 확인

버그 수정을 제외한 작업 중 아래 경우에는 작업 시작 전 접근 방식을 사용자에게 먼저 설명하고 확인을 받는다.

- 작업 범위가 모호한 경우
- 영향 범위가 넓은 변경이 예상되는 경우
- 작업 시작 시 .claude/tasks/known-issues.md를 검토하고 현재 작업과 관련된 이슈가 있으면 사용자에게 언급한다

## 작업 중단 원칙

- 아래 경우 즉시 멈추고 다시 계획을 세운다. 확신 없이 밀어붙이지 않는다
  - 동일한 에러가 반복 발생하는 경우
  - 변경 범위가 예상보다 크게 확대되는 경우
  - 접근 방식이 잘못됐다고 판단되는 경우

## 완료 전 검증

- 작동한다는 것을 증명하기 전까지 작업 완료로 표시하지 않는다
- 테스트 실행, 로그 확인, 정확성 입증을 거친다
- 스스로에게 물어본다: "시니어 엔지니어가 이걸 승인할까?"
- 작업 완료 시 변경사항이 .claude/tasks/known-issues.md의 기존 이슈에 영향을 주면 해당 항목을 업데이트한다
- 작업 중 새로운 TODO/FIXME를 발견하면 즉시 .claude/tasks/known-issues.md에 추가한다

## 테스트

- 프로젝트에 테스트 환경이 구성되어 있는 경우, 기능 구현 및 버그 수정 시 관련 테스트 코드를 함께 작성한다
- 기존 테스트가 실패하는 변경 시 사용자에게 사전 고지 후 확인을 받는다. 고지 없이 진행하지 않는다
```

---

## git-workflow.md

```markdown
# Git Workflow

## 브랜치 전략

- Git Flow를 따른다
- 영구 브랜치: main, develop
- main, develop에 직접 commit 및 push 금지
- 브랜치 유형별 네이밍:
  - feature/{설명}: 기능 구현 (develop 기준, 예: feature/login-ui)
  - fix/{설명}: 버그 수정 (develop 기준, 예: fix/token-expiry)
  - refactor/{설명}: 리팩토링 (develop 기준, 예: refactor/auth-module)
  - release/{버전}: 배포 준비 (main 기준, 예: release/1.2.0)
  - hotfix/{설명}: 긴급 배포 수정 (main 기준, 예: hotfix/payment-crash)

## 커밋 원칙

- 기능 구현 / 버그 수정 / 설정 변경 / 리팩토링은 각각 별도 커밋으로 분리한다
- git add . 사용 금지. 변경 파일을 명시적으로 지정한다
- 커밋 전 아래 항목을 예외 없이 확인한다:
  - 디버깅용 코드(console.log, print, debugger 등) 잔존 여부
  - .env, 빌드 산출물, 로그 파일의 staging 포함 여부
  - staged 파일이 이번 작업 범위와 관련 있는지 여부

## 커밋 메시지 형식

형식: {prefix}: {한국어 설명}

- feat: 새로운 기능 구현
- fix: 버그 수정
- chore: 설정, 의존성 등 기타 변경
- refactor: 동작 변경 없는 코드 개선
- style: 포맷, 공백 등 스타일 변경
- docs: 문서 변경
- test: 테스트 추가 및 수정

예시: feat: 로그인 페이지 UI 구현 / fix: 토큰 만료 시 리다이렉트 누락 수정 / chore: ESLint 설정 추가

## commit / push 규칙

- 작업 완료 후 commit은 선택이 아니라 필수다. 어떠한 경우에도 commit 없이 작업을 종료하지 않는다
- push는 사용자가 명시적으로 요청한 경우에만 수행한다
```

---

## work-history.md

```markdown
# 작업 히스토리

## 결정사항

<!-- 작업 중 팀과 합의된 중요 결정사항을 누적 기록 -->

## 알려진 이슈

프로젝트 내 TODO, FIXME를 수집하여 .claude/tasks/known-issues.md에 기록하고 관리한다.
```

---

## self-improvement.md

```markdown
# 자기개선

- 사용자에게 수정을 받을 때마다 해당 패턴을 .claude/rules/principles/lessons.md에 기록한다
- .claude/rules/principles/lessons.md가 존재하는 경우 세션 시작 시 먼저 검토하고 같은 실수를 반복하지 않는다
```

---

## lessons.md

```markdown
# Lessons Learned

## 작업 프로세스
```

---

## command-principles.md

```markdown
# 명령어 작성 원칙

섹션에 관계없이 모든 작업에서 아래 원칙을 예외 없이 따른다.

- &&, ||, |, ;, > 등 셸 연산자가 포함된 합성 명령어는 사용하지 않는다. 각 명령어를 순서대로 개별 실행한다
- 커밋 메시지에 $()나 백틱을 사용하지 않는다. 줄바꿈이 필요한 경우 -m 옵션을 여러 번 사용한다
  예시: git commit -m "feat: 로그인 구현" -m "- 소셜 로그인 추가" -m "- 세션 처리 개선"
```
